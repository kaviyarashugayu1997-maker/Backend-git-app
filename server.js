const PORT = 5000;
// Using '0.0.0.0' tells the app to accept external connections
// app.listen(PORT, '0.0.0.0', () => {
//     console.log(`Server is running on http://13.63.137.41:${PORT}`);
//     });
//
//     // Add this so the browser doesn't show "Cannot GET /"
//     app.get('/', (req, res) => {
//         res.send('<h1>The Deployment Worked!</h1><p>Your app is now live on EC2.</p>');
//         });
